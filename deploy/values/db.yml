#spec:
#  env:
#  - name: DB_USER
#    valueFrom:
#      secretKeyRef:
#        name: db-secret
#        key: db-user
#  - name: DB_PASSWORD
#    valueFrom:
#      secretKeyRef:
#        name: db-secret
#        key: db-password

mongodb:
  uri: "mongodb://dbuser:dbpassword@db-service:27017/admin?authSource=admin"
serviceMonitor:
  interval: 30s
  scrapeTimeout: 10s
  enabled: true
  additionalLabels:
    release: prometheus

#  template:
#    spec:
#      containers:
